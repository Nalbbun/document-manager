# Document Manager v1.1 기능 정리

이 문서는 `dev1.1` 브랜치에 구현된 기능을 기능별로 정리합니다.

## 1. 문서 등록

- PDF, Markdown, TXT 파일 업로드
- 업로드 진행률 표시
- 서버 로컬 폴더 경로 기반 문서 등록
- 폴더 가져오기 작업 중 진행 상태 표시
- 허용 확장자 및 최대 업로드 크기 설정
- SHA-256 해시 기반 중복 파일 등록 차단
- 폴더 가져오기 보안 정책 적용
  - 기본 가져오기 루트: `data/import`
  - 절대 경로 허용 여부 설정
  - Windows 시스템 폴더 차단
  - 숨김 파일 제외
  - 심볼릭 링크 차단 또는 허용
  - 최대 파일 수 및 전체 크기 제한

## 2. 문서/폴더 관리

- 폴더 생성, 이름 변경, 삭제
- 문서 관리와 폴더 관리를 하나의 메뉴로 통합
- 폴더 클릭 시 해당 폴더 안의 문서 목록 표시
- 문서명 변경
- 문서 폴더 이동
- 문서 휴지통 이동
- 선택 문서 일괄 이동
- 선택 문서 일괄 삭제
- 선택 문서 일괄 태그 적용
- 문서 태그, 메모, 즐겨찾기, 고정 관리
- 문서 목록 정렬, 빠른 필터, 페이지네이션, 컬럼 표시 설정
- 중복 문서 그룹 조회

## 3. 문서 보기

- PDF, Markdown, TXT 모달 뷰어
- 검색 결과에서 바로 문서 모달 열기
- TXT/MD 라인 기반 하이라이트
- PDF 뷰어 페이지 이동 및 브라우저 기본 검색 프래그먼트 연결
- 원본 파일 새 창 열기

## 4. 검색

- 전체, 폴더, 문서 단위 검색
- 확장자 필터
- 대소문자 구분
- 정확 일치
- AND, OR, 문구 검색
- 제외어 검색
- 태그, 즐겨찾기, 고정 필터
- 관련도, 최신순, 문서명순 정렬
- 검색 결과 하이라이트
- 검색 결과 페이지네이션
- 검색 이력 저장, 재실행, 삭제
- 검색 이력 보존 개수 설정
- 검색 결과 CSV 내보내기
- 검색 결과 Markdown 내보내기

## 5. 인덱스 관리

- 전체 인덱스 상태 조회
- 전체 인덱스 재생성
- 파일별 인덱스 재생성
- 인덱스 재생성 중 작업 상태 표시
- 데이터 무결성 검사
- 자동 복구
- 복구 전 안전 백업 생성

## 6. 휴지통

- 삭제 문서 휴지통 이동
- 휴지통 목록 조회
- 선택 폴더로 복원
- 복원 시 충돌 정책 선택
  - `block`: 같은 파일명이 있으면 복원 차단
  - `auto_rename`: 같은 파일명이 있으면 `_restored` 이름으로 자동 복원
  - `select_folder`: 선택 폴더 기준으로 충돌 확인
- 휴지통 항목 영구 삭제
- 휴지통 비우기
- 휴지통 보존 기간 설정값 추가

## 7. 백업/복원

- 전체 데이터 ZIP 백업 생성
- 백업 목록 조회
- 백업 ZIP 다운로드
- 백업 파일 구조 검증
- 백업 SHA-256 해시 기록 및 검증
- 백업 미리보기
  - 문서 수
  - 폴더 수
  - 검색 인덱스 수
  - 휴지통 수
  - ZIP 엔트리 수
- 복원 드라이런
- 복원 전 `PRE_RESTORE` 안전 백업 생성
- 복원 실패 시 안전 백업 기반 롤백 시도
- 백업 보존 개수 및 보존 기간 설정값 추가

## 8. 로그 관리

- 앱 로그, 오류 로그, 감사 로그 조회
- 로그 키워드 검색
- 로그 레벨 필터
- 로그 다운로드
- 로그 보관 후 현재 로그 비우기
- 로그 삭제
- 로그 보존 기간 설정값 추가

## 9. 환경 설정

- 저장소 루트 경로
- 인덱스 루트 경로
- 로그 루트 경로
- 휴지통 루트 경로
- 백업 루트 경로
- 가져오기 루트 경로
- 허용 확장자
- 최대 업로드 크기
- 최대 가져오기 파일 수
- 최대 가져오기 전체 크기
- 기본 폴더명
- 미분류 폴더명
- 검색 하이라이트 사용 여부
- 감사 로그 사용 여부
- 절대 경로 가져오기 허용 여부
- 심볼릭 링크 따라가기 여부
- 숨김 파일 제외 여부
- 중복 충돌 기본 정책
- 백업/휴지통/로그/검색 이력 보존 설정
- 무결성 검사 후 자동 복구 설정값

## 10. UI/UX

- 작업 중 메뉴 이동 잠금
- 작업 상태 패널 표시
- 업로드/가져오기/인덱스/복원 등 장시간 작업 상태 표시
- 성공, 경고, 오류, 정보 Toast 알림
- 결과 실패 목록 상세 표시
- 반복 작업용 테이블, 필터, 페이지네이션, 아이콘 버튼 구성

## 11. 안정화 보강

- JSON 저장소 원자적 쓰기 강화
- 임시 파일 JSON 재읽기 검증 후 교체
- 설정 파일 백업 및 검증 저장
- 폴더 가져오기 경로 보안 강화
- 백업 검증, 미리보기, 드라이런, 롤백 흐름 보강
- 휴지통 복원 충돌 정책 보강
- 로그 운영 기능 보강
- 검색 결과 파일 내보내기 보강

## 12. 검증 결과

```text
Backend compile: python -m compileall backend\app
Frontend build:  pnpm run build
```

두 검증 모두 통과했습니다.
